;; produced by cvc5.drv ;;
(set-logic ALL)
(set-info :smt-lib-version 2.6)
;;; generated by SMT-LIB2 driver
;;; SMT-LIB2 driver: bit-vectors, common part
(declare-sort-parameter a)
(declare-sort-parameter a1)
(declare-datatypes ((poly 1))
  ((par (a1) ((A (A_proj a1))))))

;; "p"
(declare-fun p ((poly a1)
  (poly a1)) Bool)

;; "G"
(assert (forall ((x (poly a1)) (y (poly a1))) (p (A x) (A y))))

;; Goal "g"
;; File "bench/check-ce/polymorphism.mlw", line 19, characters 7-8
(assert
  (not (forall ((x (poly Int))) (= x (A 0)))))

(check-sat)

sat
(
  ;; universe for t:
  ;;   t!val!0 t!val!1 t!val!2 
  ;; -----------
  ;; definitions for universe elements:
  ;(declare-fun t!val!0 () t)
  ;(declare-fun t!val!1 () t)
  ;(declare-fun t!val!2 () t)
  ;; cardinality constraint:
  ;(forall ((x t)) (or (= x t!val!0) (= x t!val!1) (= x t!val!2)))
  ;; -----------
  (define-fun b () t
    t!val!1)
  (define-fun a () t
    t!val!0)
  (define-fun f ((x!0 t)) t
    t!val!2)
)
